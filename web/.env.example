# Base URL the browser hits for the API. Defaults to the Vite dev-server
# proxy at /api which forwards to localhost:8000 (see vite.config.ts).
# VITE_API_BASE_URL=/api

# Dev bearer token. Must match the API's IPPON_DEV_TOKEN env.
# VITE_IPPON_DEV_TOKEN=changeme
